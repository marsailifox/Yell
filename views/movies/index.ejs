<%- include('../partials/header') %>

<table id="list">
  <thead>
    <tr>
      <th>Post</th>
    </tr>
  </thead>
  <tbody>
    <% movies.forEach(function(m) { %>
      <tr>
        <td><%= m.title %></td>
        <td><a href="/movies/<%= m._id %>">Comments</a></td>
      </tr>
      <% }); %>
    </tbody>
  </table>
  
<%- include('../partials/footer') %>
